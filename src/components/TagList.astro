---
import Tag from "./Tag.astro";

export type Props = {
  tags: string[];
};
const { tags } = Astro.props;
---

<div>
  {tags.map((tag) => <Tag tag={tag} />)}
</div>

<style>
  div {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
  }
</style>
